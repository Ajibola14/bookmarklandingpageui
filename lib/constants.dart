import 'package:flutter/material.dart';

Color softBlue = const HSLColor.fromAHSL(1, 231, 0.69, 0.60).toColor();
Color softRed = const HSLColor.fromAHSL(1, 0, 0.94, 0.66).toColor();
Color grayishBlue  = const HSLColor.fromAHSL(1, 229, 0.08, 0.60).toColor();
Color darkBlue  = const HSLColor.fromAHSL(1, 229, 0.31, 0.21).toColor();
